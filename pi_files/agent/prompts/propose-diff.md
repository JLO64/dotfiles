---
description: Use the diff-proposer subagent to draft reviewable diffs without applying changes
argument-hint: "<change request>"
---
Use the subagent tool with agent `diff-proposer` to propose, but not apply, a minimal reviewable diff for:

$@

The subagent must not edit or write files. Return only proposed changes, diffs, rationale, and risks/follow-up.
